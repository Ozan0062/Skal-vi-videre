@inherits LayoutComponentBase

@inject NavigationManager NavigationManager
@using Skal_vi_videre.Repository
@using Microsoft.AspNetCore.Authentication
@using Microsoft.AspNetCore.Authentication.Cookies
@inject IHttpContextAccessor HttpContextAccessor


<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <!-- Title with custom style -->
        <a class="navbar-brand custom-title" href="/">
            Skal vi videre?
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                @if (IsCompanyLoggedIn)
                {
                    <!-- Logged-in View -->
                    <li class="nav-item">
                        <button class="btn btn-primary nav-link fw-bold bordered-element" @onclick="NavigateToEvents">
                            <i class="fas fa-list"></i>
                            Mine Events
                        </button>
                    </li>
                    <li class="nav-item">
                        <button class="btn btn-primary nav-link fw-bold bordered-element" @onclick="NavigateToCreateEvent">
                            <i class="fas fa-calendar-plus"></i>
                            Opret Event
                        </button>
                    </li>
                    <li class="nav-item">
                        <button class="btn btn-basic nav-link fw-bold bordered-element" @onclick="LogOut">
                            <i class="fas fa-sign-out-alt"></i>
                            Log ud
                        </button>
                    </li>
                }
                else
                {
                    <!-- Not Logged-in View -->
                    <li class="nav-item">
                        <button class="btn btn-outline-primary me-2" @onclick="NavigateToCreatePage">
                            <i class="fa-plus-circle"></i>
                            Opret virksomhed
                        </button>
                    </li>
                    <li class="nav-item">
                        <button class="btn btn-outline-primary" @onclick="NavigateToLogInPage">
                            <i class="fa-sign-in-alt"></i>
                            Login
                        </button>
                    </li>
                }
            </ul>
        </div>
    </div>
</nav>





<div class="container-fluid">
    <div class="row">
        <div class="col-lg-12">
            @Body
        </div>
    </div>

    @* <footer class="footer">
        <div class="container">
            <p>Footer</p>
        </div>
    </footer> *@
</div>

<div id="blazor-error-ui">
    An unhandled error has occurred.
    <a href="" class="reload">Reload</a>
    <a class="dismiss">🗙</a>
</div>

@code {
    private bool IsCompanyLoggedIn => Skal_vi_videre.Components.Pages.LogIn.LoggedInCompany != null;

    private void NavigateToLogInPage()
    {
        NavigationManager.NavigateTo("/LogIn");
    }

    private void LogOut()
    {
        NavigationManager.NavigateTo("/logout");
    }

    private void NavigateToCreatePage()
    {
        NavigationManager.NavigateTo("/Register");
    }

    private void NavigateToCreateEvent()
    {
        NavigationManager.NavigateTo("/CreateEvent");
    }

    private void NavigateToEvents()
    {
        NavigationManager.NavigateTo("/Events");
    }
}
